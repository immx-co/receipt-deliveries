spring.application.name=receipt-deliveries-backend

server.port=8080

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5454/receipt_deliveries}
spring.datasource.username=${DB_USERNAME:user}
spring.datasource.password=${DB_PASSWORD:password}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

management.endpoints.web.exposure.include=health,info

security.jwt.issuer=receipt-deliveries
security.jwt.access-token-ttl=PT2H
security.jwt.secret=${JWT_SECRET:change-this-development-secret-at-least-32-bytes}